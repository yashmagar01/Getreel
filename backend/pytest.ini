[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = .
addopts = --ignore="Test files"
markers =
    live: hits live Instagram / streaming APIs (needs network + cookies); skipped unless RUN_LIVE=1
    unit: fast offline tests of deterministic layer logic (no network)